# 压缩、遗忘与消息模型：交接与待办

> **这是索引，不是提案。** 它只回答「现在走到哪、下一步是什么、谁在等谁」。每条设计的**理由**都在它自己那份提案里，这里只给指针，不复制论证——复制出来的两份迟早会分叉。
>
> 起于 2026-09-19 的一串设计讨论。事实以[运行架构](../../architecture.md)、[交互模型](../../interaction-model.md)、[部署与测试风险](../../runtime.md)为准。

## 怎么用这份

- **要推进** → 第三节（任务关系与分阶段交付）。
- **走到需要判断的地方** → 第四节（到站询问计划；没到站就不问）。
- **想知道为什么这么定** → 第二节给出定它的人和它所在的提案。
- **这份没写的未决** → 第七节，那些留在各自提案里，没有搬过来。

判断的归属一律标明：**草籽定**＝他的判断；**推论**＝这份的作者从代码或前提推出来的，没有经过他确认。

---

## 一、已落地，但**都还没重启**

| 提交 | 内容 | 生效条件 |
|---|---|---|
| `000ca9c` | 私聊窗口改由 `target_id` 给出，`user_id` 一律是作者 | 已在 main |
| `821d1e5` | 自发消息回声接管记账；`_route` 关卡改「记录、不派发」；`record_sent` 与两个调用点删除 | **需重启** |
| `1948d56` | `oplog.sweep` 拆掉；边界不再物理删除；过滤下推 `build_rounds(since=)` | **需重启** |

后两笔改的是**记账权威**和**删除路径**，堆着不验风险偏高。建议待办 1 落完就重启一次，三笔一起验：

- 私聊两个方向、群聊、合并转发各自在 chatlog 里只出现一次，窗口正确（`821d1e5`）
- `recall_ops` 能取回「聊天开始」之前的 cid（`1948d56`）

详见[自发消息回流](self-message-echo.md)与[聊天消息的递归压缩](chat-condense.md)第 5 条。

## 二、这串讨论里定下来的

### 草籽定的

1. **发言工具化是正解，不走兼容路。** 「给 `msg2chat` 的 assistant 分支加 metadata 让模型看见自己的 `message_id`」被否——出发点是怎么做更好，不是怎么做更兼容。
2. **模型的 assistant content = 自言自语**，不再直接发出去。`chat.get_handler` 里 `message.sendmsg(chunk.content)` 那行随之消失。
3. **自言自语不跨轮。** 理由：跨轮就要考虑追踪，而它是没追踪的东西。
4. **同一句话出现两遍不是冗余**，`say` 那次调用和窗口里那条消息**含义不同**：前者是「我做了一个动作」，后者是「这个窗口里出现了一句话」。调用失败时只有前者有记录，别的客户端说话时只有后者有。
5. **触发与工具返回解耦。** mail 是 queue／缓冲区：工具回调、别人发的消息都 `add` 进去，只有特定信号才让缓冲区进入**激活状态**（目前是戳一戳、at、「柚子，」开头，以及工具调用返回）。**工具返回不该是触发信号。**
6. **激活状态下的「创建会话／插话」统一**——这就是消息模型统一要做的事。
7. **不单独做「等消息的原语」**（2026-09-20 确认）。早先提议加一个「在本轮内等下一条消息」的工具，好让模型在动手前能等一句确认而不必靠结束这一轮。第 5 条把它吸收掉了：会话本来就不死、只是不激活，所以「等」是**默认状态**而不是一个动作。那颗螺丝不用拧，`context.WindowTurn._pending` 那套随待办 3 一并重做。

### 从上面推出来的（推论，未经草籽确认）

8. **三层寿命**，各有各的生命周期，不需要额外规则协调：

   | 层 | 是什么 | 寿命 |
   |---|---|---|
   | assistant content | 自言自语 | 轮内，轮结束就死 |
   | `say` 调用 | 我说话这个动作 | oplog 轨道，按高度退休 |
   | chatlog 那条 | 窗口里存在这句话 | 永不删 |

   这是草籽两条线框架（见 [chat-condense](chat-condense.md) 的「问题：两条线，别混」）的直接推论：工具调用按高度退休、聊天消息永不删，两个句柄各走各的线。

9. **工具化顺带解掉一个现存问题：刷屏。** `get_handler` 是「每个 chunk 直接发一条」，所以模型一段话会变成好几条 QQ 消息。工具化之后，想多少是它的事，说几句是它显式决定的。

## 三、推进规则与任务关系

### 问题不一次问完

每一阶段按同一个次序推进：

1. 先从代码、现行文档和安全的合成实验里回答事实问题；
2. 再把仍会改变方案的部分收成一个决策闸门；
3. 只在下一项实现真的被它挡住时问草籽，并同时给出证据、可选语义和各自后果；
4. 得到决定后完成这一段实现与验证，再进入下一个闸门。

不能改变当前任务的远期问题继续停放，不借一次讨论把后面所有选择一起搬来。实现、合并、运行时激活和真机观察也是四个不同阶段；尤其是重启与物理删除，不能被「代码已经写好」自动授权。

### 依赖图

这里保留原待办号，但把待办 4 拆成三个有先后的切片。箭头表示语义或实现前置，不表示必须在同一次提交完成。

```text
待办 1a：从 init_chat 摘出显式生命周期 hook（时机先不变）
    └─ 运行时激活与旧改动真机核验（需另到站，不阻塞继续做设计）

未决 A：一轮的终止语义
    ├─ 未决 J → 待办 2 → 待办 4a → 未决 C+D → 待办 4b → 未决 B+H → 待办 4c
    └─ 待办 3a → 待办 3b（接管待办 1a 的最终时机）

待办 4c
    ├─ 待办 7a：撤聊天边界
    └─ 未决 E → 待办 5 → 未决 F-0 → 退休 dry-run → 未决 F-1（选 H） → 未决 F-2（开启真删） → 待办 6 → 待办 7b（撤 #ops clear）

未决 G：同账号其它客户端的消息语义（独立旁路，不阻塞上述主线）
```

待办 1 与待办 3 不是原表里的单向依赖：`inc_call_count` 与工具恢复现在都挂在 `init_chat`，而最终应该何时发生取决于未来的会话／激活生命周期。待办 1 因此先只抽出有名字的 hook、保持调用时机与结果不变；待办 3 再让新生命周期接管它们，避免提前猜一个时机再迁第二次。

待办 2 与待办 3 都被 A 挡住；实施时仍先做 2、再做 3，因为二者都触碰聊天循环，而 2 先把「说话动作」和「轮何时结束」定成稳定生产者，3 再重做激活与插话，能避免同时迁移两套控制语义。待办 3 目前还**没有自己的成文设计**：原表指向的 [message-model](message-model.md) 已经七步全部实施，内容是 chatlog 双向恢复，不包含 mail 或激活状态。阶段 3 必须先补一份聚焦的设计记录，不能拿那个链接冒充理由已经存在。

待办 4 不依赖 mail 全部完成，但按主线顺序排在 3 后，先把聊天循环收稳再建立长期索引。待办 7 拆成两项：聊天边界可在索引和缓存锚接管后撤；`#ops clear` 是当前唯一人工清除入口，要等高度退休验证完成后才能撤。

### 分阶段交付

| 阶段 | 本阶段做什么 | 进入条件 | 离开条件 |
|---|---|---|---|
| **0. 显式生命周期 hook** | 待办 1a：把 `tools.restore` 的回收和 `inc_call_count` 的记账从 `init_chat` 混合装配中摘成有名字的动作；调用时机、次数与现行 ttl 语义先不变 | 无 | 行为逐字保持；副作用有单一显式入口，阶段 3 可接管时机；不以「已经抽函数」假称生命周期已定 |
| **0b. 激活核验** | 在维护者允许重启之后，让 `000ca9c`、`821d1e5`、`1948d56` 与待办 1 一起进入运行进程，做第一节列出的真路径观察 | 阶段 0 完成；另有重启许可与旧线程交接 | 私聊双向、群聊、合并转发只记一次且窗口正确；边界前 cid 仍可反查；失败则先修事实，不进入不可逆回收 |
| **1. 终止语义** | 用假的 LLM client/tool 调查当前无工具、工具成功／失败、插话、取消与续轮的结束路径；到站只问 A | 阶段 0 可独立完成 | A 得到明确语义，足以判断一轮何时结束、何时只是未激活地等待 |
| **2. 发言工具化** | 先到站问 J，再做待办 2：加入 `say`，assistant content 只作轮内自言自语，删除 `get_handler` 的直接发送，按 A 落终止；费用记账仍保留 | 阶段 1 | 说话动作有 cid，发送失败与确认时点明确，且能关联窗口回声的 `message_id`；窗口回声仍是观察权威；不保留旧发送旁路 |
| **3. 激活统一** | 待办 3a 先补写 mail／激活设计；待办 3b 再让 mail 成为缓冲区，消息与回调只负责 `add`，信号才改变激活状态；统一创建会话与插话，重做 `WindowTurn._pending`，并接管阶段 0 的 hook 时机 | 阶段 0、1；实施顺序在阶段 2 后 | 工具返回本身不触发；戳一戳、at、名字开头等既定信号仍能激活；等待是默认未激活状态；尾缘触发不丢、续轮按最后触发者切换权限、建上下文与清 mail 不重复也不漏消息 |
| **4. 索引地基** | 待办 4a：节点不受聊天预算 `floor`；计算覆盖时显式读取已收缩节点；补一条面向模型的统一投影与按 `message_id` 反查路径，继续过滤 `#` 输出和已撤回消息 | 阶段 2 | 预算收紧不会让节点蒸发；上层压掉下层节点不会让其覆盖的消息复活；反查不会让模型看见它当年没见过的内容；不在这一步引入新工具 |
| **5. 深度与缓存** | 先做 C+D 的合成测量与联合决策，再做待办 4b；同时确定撤边界所需的缓存锚 | 阶段 4 | 压缩释放的空间确实能换来更早上下文；老端移动规则可解释、可测；边界撤除不再导致每轮前缀失效 |
| **6. 第一层树** | 先收敛 B+H 的覆盖例外，再做待办 4c：一次调用同时收 `message_ids` 与 `cids`，并提供同投影反查 | 阶段 5 | 一层节点能生成、隐藏、反查；`say` 动作与它对应的窗口观察能一起离开上下文；反查不解除覆盖；chatlog 与 oplog 仍各自是事实权威 |
| **7-A. 撤聊天边界** | 待办 7a：撤「聊天开始／结束」的可见性边界；保留 `#ops clear` / `oplog.clear` | 阶段 6，且阶段 5 已给出缓存锚 | 分段与缓存职责被索引和新锚接管；人工清除入口仍在，直到退休路径验证完成 |
| **7-B. 主动压缩** | 先用已落地的一层树观察，再到站问 E，随后做待办 5；压力信号先只建议最老未覆盖段及代价 | 阶段 6 | 模型会在硬上限前得到可执行且稳定的压缩建议，树能够持续增长；信号不自行写结论或删除记录 |
| **8. 遗忘** | 先到站解决 F-0 的结构语义，再实现 report-only 的高度退休并观察；F-1 选 H，F-2 才允许真删。待办 6 验证后，待办 7b 才撤 `#ops clear` / `oplog.clear` | 阶段 7-B | 树／DAG、高度、引用与退休状态定义一致；选定 H 有数据依据；能区分「已退休」与「从未存在」；真实删除有单独启用闸门、可观察结果和关闭路径；旧 clear 最后才退役 |

阶段 7-A 与 7-B 在语义上可以分开：前者处理旧边界与缓存，后者让树主动生长。实现仍按实际代码重叠串行，避免两边同时改 `chat.py` / `oplog.py`。

## 四、到站询问计划

下表不是现在要回答的问题清单，而是**何时才允许把问题交给草籽**。前面的调查能直接排除问题时，就记录结论并跳过询问。

| 闸门 | 到站前由实现者完成 | 到站时只确认 | 放行 |
|---|---|---|---|
| **I（阶段 0 内）** | 核对 `.md` 模块是否存在能代表「仍在使用」的现成动作，并对照 `SessionBinding.restore` 的现行 WHY。当前代码已经把「阅读不留痕迹、到期仍收回」写成知情取舍 | 只有出现新的真实反例、或待办 1 必须改变 ttl 语义时，才问是否推翻这个取舍；否则保持现状，不问 | 待办 1；I 不再伪装成默认阻塞项 |
| **A（阶段 1）** | 列出无工具、`say`、其它工具、工具失败、插话、`^C`、重启续话的完整状态序列，确认哪些是当前代码事实，准备最小候选语义 | 什么可观察事件表示「这一轮已完成」；是否需要显式结束动作。一次只拍这个语义，不连带询问压缩策略 | 待办 2、3 |
| **J（阶段 2）** | 对 `message.sendmsg` / `SendFuture`、NapCat 回声与 oplog 记账做合成时序；列出排队成功、API 确认、回声到达和发送失败时分别拥有什么 id，先排除无法可靠关联的方案 | `say` 在哪个时点算动作完成、失败怎样回给模型，以及 cid 怎样与随后进入 chatlog 的 `message_id` 关联。它只处理发言动作，不和 A 的整轮终止混问 | 待办 2、待办 4c |
| **C+D（阶段 5）** | 用合成消息量化「数原始／数可见／退役硬条数上限」各自的回溯深度和老端移动；验证实际供应商缓存命中字段能否观测，不读真实聊天正文 | `max_msg` 的职责，以及撤边界后愿意采用哪一种可解释的缓存锚。两题共同决定同一段选取算法，合并问一次 | 待办 4b、7 |
| **B+H（阶段 6）** | 枚举没有 cid/message_id 的 Bot 非模型消息和戳一戳在真实投影中的位置，确认它们只是留下少量不可覆盖行，还是会破坏成段压缩 | 是否接受这些事件不参与点名压缩；若不接受，再分别讨论给哪一类补稳定身份。没有影响就不扩成统一事件 ID | 待办 4c 的覆盖范围 |
| **E（阶段 7b）** | 先让一层节点可用；用合成轨迹或显式授权的观察统计段大小、工具调用数量、token 与缓存代价，比较候选压力信号 | 信号在什么压力下出现、一次点名多大的最老未覆盖段，以及只提醒还是形成更强约束 | 待办 5 |
| **F-0（阶段 8，report-only 前）** | 用纯内存图覆盖链、分支、多父引用、重复压缩、recall 提升与 clear。那个「整棵子树删除 vs 被引用原文不能删」的冲突**已由草籽 2026-09-20 裁掉**：删除的优先级必然低于保存的优先级，因为层级提升靠反查，整棵子树删必然误伤引用。于是退休＝旧 `sweep` 的可达性回收换一个根（根从「窗口内的轮」改成「高度 ≤ H 的节点」，遍历不变），`_referenced_cids` 就是接管者本身而不是等复用的零件；见 [chat-condense](chat-condense.md) 第 3 条 | 剩两问：**多父时高度怎么算**（影响已经小了——高度只用来选根，不用来判单个节点死活；倾向取最小高度，即离根最近那条路径）；**用什么最小事实区分「已退休／从未存在」**（tombstone 或别的）。已答：允许 DAG；超过高度后被引用的原文**不删** | 退休 dry-run 的定义 |
| **F-1（阶段 8，report-only 后）** | 实现不删除的退休报告，展示不同 H 会命中的子树、工具原文量与失去索引路径的聊天范围；运行时观察另行授权 | 依据报告选择 H，或决定继续只报告 | 待办 6 的参数 |
| **F-2（真删前）** | 验证 dry-run 口径、备份／恢复边界、已退休与未存在的错误语义，以及关闭真删后不继续扩大影响 | 是否正式开启物理删除。选择 H 不等于授权删除 | 待办 6 的激活 |
| **G（独立旁路）** | 先查 NapCat 协议或做经授权的受控观察，确认其它客户端发言实际得到什么 `post_type` / `message_sent_type`；不要靠字段名猜 | 只有证明确有可区分事件后，才问它在模型上下文里算 Bot 发言、用户发言还是单独来源 | 不阻塞主线；只影响同账号多客户端语义 |

其它散落未决继续留在各自提案里，只有任务碰到它们时才按同一规则提升。尤其是[统一消息模型](message-model.md)已经实施后的剩余问题，不因本主线存在就自动重新打开。

## 五、记着，现在不做

这两条是**同一条线的两端**：都用结构代替转义，一个治 CQ，一个治工具调用。

- **消息正文换数组格式。** 解决 CQ 转义，需要改设备上的 NapCat 配置，属于部署侧决定。见[消息表示](message-representation.md) §4.B 与 §5 第 6 条。
- **工具调用换 LTF**（行式类型格式，草籽另有 v0.1 草案）。它的多行字符串靠「每行以 `|` 开头」这个结构性质，不靠转义，所以不可注入；而且**每一行只凭自己就能解析**，于是工具调用也能流式。
- **流式观感的损失**：工具化之后工具调用要等整个结构生成完才能发，逐字出现的观感没了。**草籽判定目前可接受**，LTF 落地时自然解决。

## 六、散落的小缺口

都不紧急，但会让下一个人多走弯路：

- **`oplog.build_messages` 是死代码**（0 个调用者）。删之前要先决定它 docstring 里那段 `reasoning_content` 的实测结论搬去哪。
- **`docs/working/current-issues.md` 的「已确认的缺陷」一节已经过期。** 它把「工具超时、`^C` 真去杀子进程、`pending_calls` 循环每次调用前查 `should_stop`」列为未决；后两件已经做了（`mods/watchdog.py`，以及 `mods/llm/__init__.py` 那三处 `should_stop` 检查），只剩**工具超时**。
- **`message.recvmsg()` 仍把作者与窗口写成一个值**（`sender_id` 同时写进顶层 `user_id` 与 `sender.user_id`），所以它表达不了「作者 ≠ 窗口」。`000ca9c` 改完别处之后，它是这条约定唯一没跟上的入口。

## 七、这份**没有**收录的未决

各自留在原处，动到那一块时去读，不要只看这份：

- [消息表示](message-representation.md) **§5** 的六条：入口判定以哪种文本为准、link 条件面向哪种文本、模型输入渲染到什么程度、模型输出是否换通道、chatlog 文件格式是否跟着变、是否走数组格式。
- [统一消息模型](message-model.md) **§未决**：窗口键的拼写、浅路径 `getlog()` 是否返回拷贝、`chatlog/bot/` 的定位、终端回显的确切样子、切换前私聊戳一戳能否恢复方向、notice 其余家族是否结构化。
- [当前维护队列](../current-issues.md)：link helper 对多页图片 URL 的旧改写语义、是否在所有 LLM 入口统一要求触发者为 op。

代码里的 `WHY?:` 队列（`grep -rn "WHY?:" mods/`）**当前为空**——没有未答的、代码内的开放问题。
